# Current UI prototype needs no secrets or database connection.
PORT=3000
HOST=0.0.0.0
# Future backend only. Not read by this UI prototype.
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE
